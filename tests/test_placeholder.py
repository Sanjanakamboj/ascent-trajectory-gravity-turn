"""Milestone 1 placeholder test.

Only verifies the package skeleton imports cleanly. No ascent solver exists yet;
solver-behavior tests begin in Milestone 2.
"""

import ascent


def test_package_imports():
    assert ascent.__version__ == "0.1.0"
